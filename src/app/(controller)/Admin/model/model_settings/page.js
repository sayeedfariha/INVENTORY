import SettingsModel from '@/app/(view)/admin/model/model_settings/page';
import React from 'react';

const ModelSettings = () => {
    return (
        <div>
            <SettingsModel></SettingsModel>
        </div>
    );
};

export default ModelSettings;