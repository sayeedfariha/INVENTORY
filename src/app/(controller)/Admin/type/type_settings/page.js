import SettingsType from '@/app/(view)/admin/type/type_settings/page';
import React from 'react';

const TypeSettings = () => {
    return (
        <div>
            <SettingsType></SettingsType>
        </div>
    );
};

export default TypeSettings;